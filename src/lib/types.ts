export type Category = {
  name: string;
  slug: string;
  description?: string;
};

export type Tag = {
  name: string;
  slug: string;
};

export type Series = {
  name: string;
  slug: string;
  description?: string;
};
